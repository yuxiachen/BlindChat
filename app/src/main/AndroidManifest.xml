<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.blindchat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round_shadow"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/AppSplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.InitActivity"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".ui.LoginActivity"
            android:theme="@style/AppThemeWithActionBar"/>
        <activity android:name=".ui.SignupActivity"
            android:theme="@style/AppThemeWithActionBar"/>
        <activity android:name=".ui.PasswordResetActivity"
            android:theme="@style/AppThemeWithActionBar"/>
        <activity android:name=".ui.MainActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".ui.ChatroomActivity"
            android:parentActivityName=".ui.MainActivity"
            android:windowSoftInputMode="adjustResize"
        android:theme="@style/AppThemeWithActionBar" />
        <activity android:name=".ui.ChatroomInfoActivity"
            android:theme="@style/AppThemeWithActionBar" />
        <activity android:name=".ui.ChangeUsernameActivity"
            android:theme="@style/AppThemeWithActionBar"/>
        <activity android:name=".ui.ChangePasswordActivity"
            android:theme="@style/AppThemeWithActionBar"/>
    </application>

</manifest>